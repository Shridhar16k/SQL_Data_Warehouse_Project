# SQL_Data_Warehouse_Project
Built a scalable Data Warehouse using Databricks and Delta Lake with Medallion Architecture (Bronze, Silver, Gold). Performed data ingestion, transformation, and aggregation using SQL. Designed fact and dimension models, implemented incremental loads and SCD, and enabled reporting via Power BI with Git-based version control.
